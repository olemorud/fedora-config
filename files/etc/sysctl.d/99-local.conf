# Swap sits behind zswap on NVMe, so swapping out cold anonymous pages is
# about as cheap as dropping page cache. 100 means "equal cost"; the
# kernel default of 60 assumes swap on a slow disk. See docs/decisions.md.
vm.swappiness = 100
