# 32 GiB RAM, fast NVMe, fast CPU: one 16 GiB zstd zram device, no disk
# swap. Fedora's default caps zram at 8 GiB; half of RAM is a better fit.
# zswap is not enabled on Fedora kernels and would only get in the way here.
[zram0]
zram-size = ram / 2
compression-algorithm = zstd
