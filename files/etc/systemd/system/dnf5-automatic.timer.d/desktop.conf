[Timer]
# The stock unit fires at 06:00, when this machine is off. Clear the
# calendar, ask for once a day, and let Persistent run the missed job on
# the first boot of the day. The delay keeps the download off the heels
# of login.
OnCalendar=
OnCalendar=daily
Persistent=true
RandomizedDelaySec=30m
