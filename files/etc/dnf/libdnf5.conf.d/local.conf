[main]
max_parallel_downloads=10
# The dnf5 default, stated so it survives a change of upstream defaults:
# removing a package also removes the dependencies that were pulled in
# for it and that nothing else needs.
clean_requirements_on_remove=True
