[main]
max_parallel_downloads=10
# The dnf5 default, stated so it survives a change of upstream defaults:
# a removal by hand also drops the dependencies nothing else needs. The
# Ansible dnf5 module ignores this and uses its own autoremove flag.
clean_requirements_on_remove=True
